# Disable source map generation in production builds.
# This prevents the harmless "Failed to parse source map" warnings emitted
# by third-party dependencies (e.g. @mediapipe/tasks-vision shipped with three.js).
GENERATE_SOURCEMAP=false
